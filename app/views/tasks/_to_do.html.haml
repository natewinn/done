%table
  %thead
    %tr
      %th Trash
      %th Task name
      %th Done
  %tbody
    - @tasks.each do |task|
      %tr
        %td= button_to 'Delete', task_path(task), method: :delete, class: 'button'
        %td= task.name
        %td= check_box_tag 'receive_email', 'yes', false